apiVersion: v1
kind: Service
metadata:
  name: {{ include "todo-chat-bot.fullname" . }}
  labels:
    {{- include "todo-chat-bot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "todo-chat-bot.selectorLabels" . | nindent 4 }}
